<h1>Produkty</h1>

<%= link_to "Pridať produkt", new_admin_product_path, class: "btn btn-primary" %>

<table class="table">
  <thead>
  	<tr>
  	  <th>Meno</th>
  	  <th>Popis</th>
  	  <th>Cena</th>
  	  <th>Obrázok</th>
  	  <th></th>

  	</tr>
  </thead>

  <tbody>
<% @products.each do |product| %>
   
 <tr>
   <td><%= product.name %></td>
   <td><%= product.description.html_safe %></td>
    <td><%= product.price %></td>
     <td><%= product.image_url %></td>
   <td><%= link_to "Upraviť", edit_admin_product_path(product), class: "btn btn-primary" %> | 
       <%= link_to "Zmazať", admin_product_path(product), method: "delete", confirm: "naozaj?", class: "btn btn-primary" %></td>
 </tr>
<% end %>
  </tbody>
</table>